/*
 * abacus_utils.c
 */

#include "abacus_utils.h"

/*
 * It converts a buffer of 2 chars to a 16 integer
 */
void char2int(uint8_t *input, int *output)
{
  typedef union
  {
    int intValue;
    uint8_t byteValue[2];
  }
  assocdata;
  assocdata conversion;
  int i;
  for(i = 0; i < 2; i++)
    conversion.byteValue[i] = input[i];

  *output = conversion.intValue;
}

/*
 * It converts a buffer of 2 chars to a 16 unsigned integer
 */
void char2uint(uint8_t *input, unsigned int *output)
{
  typedef union
  {
    unsigned int intValue;
    uint8_t byteValue[2];
  }
  assocdata;
  assocdata conversion;
  int i;
  for(i = 0; i < 2; i++)
    conversion.byteValue[i] = input[i];

  *output = conversion.intValue;
}

/*
 * It converts a buffer of 2 chars to a 32 bit unsigned long
 */
void char2ulong(uint8_t *input, unsigned long *output)
{
  typedef union
  {
    unsigned long longValue;
    uint8_t byteValue[4];
  }
  assocdata;
  assocdata conversion;
  int i;
  for(i = 0; i < 4; i++)
    conversion.byteValue[i] = input[i];

  *output = conversion.longValue;
}

/*
 *
 */
void ulong2char(unsigned long *input, uint8_t *output)
{
	typedef union
	{
		unsigned long longValue;
		uint8_t byteValue[4];
	}assocdata;
	assocdata conversion;

	conversion.longValue = *input;
	int i;
	for(i = 0; i < 4; i++)
		output[i] = conversion.byteValue[i];
}

/*
 *
 */
void uint2char(unsigned int *input, uint8_t *output)
{
	typedef union
	{
		unsigned int uintValue;
		uint8_t byteValue[2];
	}assocdata;
	assocdata conversion;

	conversion.uintValue = *input;
	int i;
	for(i = 0; i < 2; i++)
		output[i] = conversion.byteValue[i];
}

/*
 *
 */
void int2char(int *input, uint8_t *output)
{
	typedef union
	{
		int intValue;
		uint8_t byteValue[2];
	}assocdata;
	assocdata conversion;

	conversion.intValue = *input;
	int i;
	for(i = 0; i < 2; i++)
		output[i] = conversion.byteValue[i];
}

/*
 * CRC8 lookup table
 */
static const uint8_t crc_table[] = {
    
};

/*
 * It calculates a CRC8 using a lookup table of a specified buffer.
 * Dont forget to add the crcT&0xFF if it is the final !!
 */
uint16_t calculate_crc8(uint8_t *p, uint8_t len, uint16_t *crc)
{
	uint16_t i;
	uint16_t crcT = *crc;

	while (len--)
	{
		i = (crcT ^ *p++) & 0xFF;
		crcT = (crc_table[i] ^ (crcT << 8)) & 0xFF;
	}

	*crc = crcT;
	return crcT & 0xFF;
}

/*
 * It calculates a CRC16 using the implemented hardware in the MSP430 which is
 * a CRC-CCITT-BR
 *  p: is the pointer to the data
 *  len: is the lenght of the data
 *  crc: is the calculated result
 *  init: 1 if it starts the crc calculation, 0 if it continues with previous data
 */
int16_t calculate_crc16(uint16_t *p, uint16_t len, uint16_t *crc, uint8_t init)
{
	//It is a new calculation? then reset
	if(init == 1)
		CRCINIRES = 0xFFFF;

	while(len != 0)
	{
		CRCDIRB = *p;
		p++;
		len--;
	}
	*crc = CRCINIRES;
	return *crc;
}

/*
 * It calculates a CRC16 using the implemented hardware in the MSP430 which is
 * a CRC-CCITT-BR, we dont want the BR (bit reverse) so we use the CRCDIRB_L
 * which is reversed but use the original output CRCINIRES for the result.
 *
 *  p: is the pointer to the data
 *  len: is the lenght of the data
 *  crc: is the calculated result
 *  init: 1 if it starts the crc calculation, 0 if it continues with previous
 *  	  data.
 */
int16_t calculate_crc16_8bit(uint8_t *p, uint16_t len, uint16_t *crc, uint8_t init)
{
	//It is a new calculation? then reset
	if(init == 1)
		CRCINIRES = 0xFFFF;

	while(len != 0)
	{
		CRCDIRB_L = *p;
		p++;
		len--;
	}
	*crc = CRCINIRES;
	return *crc;
}


